
import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

/// Base theme configs for widgets.
class ThemeConfigs {
  ThemeConfigs._();

  // colors
  static const Color primaryDarkColor = Color(0xFF6200EA);
  static const Color primaryColor = Color(0xFF651FFF);
  static const Color accentColor = Colors.amber;
  static const Color pageBackgroundColor = Color(0xFEF8F8F8);
  static const Color disabledColor = Color(0xFFB6AAFF);
  static const Color dividerColor = Color(0x66121212);
  static const Color titleColor = Color(0xff121212);
  static const Color subtitleColor = Color(0x99121212);
  static const Color normalTextColor = Color(0x88111111);
  static const Color tipTextColor = Color(0x66121212);

  // values
  static const double dividerWidth = 1.0;
  static const double buttonRadius = 12;

  // text
  static const double inputTextSize = 14;

}